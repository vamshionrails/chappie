# Layout configuration
layout {
    # Main area configuration
    
    # Sidebar configuration
    sidebar {
        width = 30%
        height = 100%
        position = 70%, 0
        background = "rgba(0, 0, 0, 0.8)"s

        # Example: Launch ChatGPT App
        window {
            exec = "/path/to/chatgpt-launcher.sh"
            floating = true
            width = 100%
            height = 100%
        }

        # Additional widgets or configurations can go here
    }
}